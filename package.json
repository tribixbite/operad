{
  "name": "operadic",
  "version": "0.3.0",
  "type": "commonjs",
  "description": "operad — manage all your Claude Code sessions from one dashboard. Web UI for skills, memories, and prompt history across projects. Auto-boots saved projects with health checks and crash recovery.",
  "author": "tribixbite",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/tribixbite/operad"
  },
  "bin": {
    "operad": "dist/tmx.js"
  },
  "files": [
    "dist/",
    "dashboard/dist/",
    "watchdog.sh"
  ],
  "keywords": [
    "claude",
    "tmux",
    "orchestrator",
    "session-manager",
    "claude-code",
    "terminal",
    "cross-platform"
  ],
  "scripts": {
    "build": "node build.cjs",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "postinstall": "node scripts/fix-android-binaries.mjs || true"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.2.104",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/ws": "^8.5.0",
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  },
  "optionalDependencies": {
    "@esbuild/android-arm64": "^0.24.0"
  },
  "engines": {
    "node": ">=18",
    "bun": ">=1.0"
  }
}
